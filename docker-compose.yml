services:
  web:
    container_name: portfolio
    image: portfolio:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/srv/jekyll
      - /srv/jekyll/node_modules
    ports:
      - "4000:4000"
